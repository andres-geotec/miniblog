{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "clean": "rm -rf .nuxt .output node_modules && rm -f package-lock.json && npm cache clean -f",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "reinstall": "npm run clean && npm i"
  },
  "dependencies": {
    "@centrogeomx/sisdai-css": "^1.8.1",
    "airtable": "^0.12.2",
    "marked": "^15.0.6",
    "nuxt": "^3.15.1",
    "vue": "latest",
    "vue-router": "latest"
  }
}
